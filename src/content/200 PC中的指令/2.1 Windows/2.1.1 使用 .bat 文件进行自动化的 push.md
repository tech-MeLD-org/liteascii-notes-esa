---
title: obasdfsadgasdgthjj        # 不填则自动取正文第一个 # 标题
date: 2026-02-12         # 用于 Timeline 排序
description:  obsidian常见问题汇总  # 用于卡片摘要
tags: [wx,jx,windows]     # 用于标签和 Thoughts 筛选
category: qq
---
在和项目根目录下新建 `auto-push.bat` 文件，具体指令可随要求修改

```
@echo off
cd /d %~dp0

echo ===== Git Auto Push =====

git add .

git diff --cached --quiet
if %errorlevel%==0 (
    echo No changes detected.
    timeout /t 3 >nul
    exit
)

for /f "tokens=1-4 delims=/ " %%i in ("%date%") do set mydate=%%i-%%j-%%k
set mytime=%time:~0,8%

git commit -m "Auto update %mydate% %mytime%"
git push origin main

echo.
echo Push successful.
timeout /t 3 >nul
exit
```
